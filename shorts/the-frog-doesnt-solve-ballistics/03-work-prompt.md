# Work Prompt — Build This Short for Substack

Use this Short package as the production basis:

- `00-manifest.md`
- `01-sources-to-load.md`
- `02-short-source.md`
- relevant current framework source/status files already available in the Work

This is a presentation-layer build, not a semantic rewrite.

## Output

Create the publication-ready Short at:

`article/the-frog-doesnt-solve-ballistics.md`

Also generate the actual editorial image for the Short and store it in the same Short package under:

`article/<generated-image-file>`

Embed the image in the Markdown using a relative link.

Do not create audio or NotebookLM files for this Short.

## Short role

Preserve the narrow mechanism:

`accumulated constraint / seated path`
→ `many alternatives no longer need fresh resolution`
→ `smaller consequential remainder`
→ `fast action without a from-scratch global calculation`

Keep the frog as the general-reader hook, not as evidence that the framework has derived animal neurobiology.

The point is that an externally describable trajectory does not have to be internally recomputed from scratch as a complete explicit solution before action.

Keep it short enough to remain a Short.

## Semantic discipline

Use the newest GitHub framework sources as authority, especially:

- `source/a-universe-made-of-logic.md`
- `source/where-the-framework-stands.md`
- `source/internal/short-notes-queue/part-05.md` for current editorial status
- `publish/guidelines.md` for publication presentation

Preserve these distinctions:

- **Path is memory.**
- Previously expensive seated structure can become reusable ground.
- Stored history is not rendered detail.
- A coarse constraint can be real while fine implementation remains unresolved.
- Present ground can make some compatible continuations cheaper to seat.
- The biological example is illustrative, not a quantitative framework derivation of motor control.

Do not claim that a frog literally performs no neural computation, that mechanics are bypassed, or that My GUT Deduction has derived animal neurobiology.

## Image generation

Generate the image directly during the Work build.

Use the image concept in `02-short-source.md` as the starting brief: a naturalistic frog and flying insect, with one clean tongue-strike path becoming definite while several very subtle unrealized arcs fade away. The image should communicate constrained continuation without becoming a diagram.

Prefer editorial landscape 16:9, naturalistic detail, restrained depth of field, and useful negative space in the upper-left or upper-center for later title placement.

Avoid embedded title text, equations, trajectory labels, grids, HUD graphics, brains, neurons, generic sci-fi glow, cosmic imagery, floating symbols, cartoon comedy, exaggerated motion blur, or classroom-physics aesthetics.

Use descriptive alt text in the Markdown.

When a caption helps, place this visible blockquote directly below the image:

> **Image Caption:** concise caption text

Keep reader-facing captions visible. Do not replace them with hidden HTML comments.

## Related-article handoff

If a related framework article should be surfaced, use:

> **Article Slot:** URL: https://...

Use only established URLs from the repository. Never invent one. If no URL is known, leave the URL empty.

## Final QA

Before finishing:

1. confirm the article matches `02-short-source.md`;
2. confirm newer framework source wins over older wording;
3. confirm Derived framework architecture and illustrative biological application remain distinct;
4. confirm the Short remains narrow and readable;
5. confirm the image is generated, stored in this package, and embedded in the Markdown;
6. confirm visible `Image Caption` / `Article Slot` blockquotes remain where useful;
7. confirm no hidden HTML production markers were introduced;
8. update `00-manifest.md` to `article-ready` when the package is complete.
