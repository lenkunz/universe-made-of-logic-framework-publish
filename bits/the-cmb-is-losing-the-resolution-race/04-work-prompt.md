# Work Prompt — Build / Correct the Bit Article

## Forced topic

**The CMB Is Losing the Resolution Race**

Slug: `the-cmb-is-losing-the-resolution-race`

## Required article arc

`standard CMB measurements`
→ `Resolution Equality`
→ `coarse vs fine arrangement`
→ `recursive nested fine growth`
→ `CMB also gets finer but more slowly`
→ `resolution-density ratio grows`
→ `frequency/temperature as Stage-side comparison`
→ `receiver/target measurement sweep`
→ `blackbody as downstream statistical map`
→ `correct blackbody audit: derive, do not preserve`
→ `cross-era scale shift as second question`
→ `anisotropy challenge`
→ `Stage connection`
→ `measurement does not carry ontology`
→ `hard audit`

## Mandatory statements

> **The CMB is not losing the universe's budget. It is losing the race toward fine resolution.**

> **The universe gives the same fundamental total resolving capacity everywhere.**

> **Fine structures grow inward in relational density. Coarse structures can remain broad in relational reach.**

> **Measurement does not carry its ontology with it.**

## Blackbody correction

This is mandatory.

Do **not** say:

> “Cross-resolution projection must preserve the Planck spectrum.”

That wording assumes the thing the framework is trying to derive.

Instead:

- `B_x` is the selected periodic comparison grain/range;
- `B_t` is the target budget available to that comparison in the shared anchor;
- frequency is the surfaced comparison result;
- `G(x)` is the resolved gain/measurement at the selected comparison;
- the spectrum is the collection of `G(x)` over the sweep;
- Planck's law statistically summarizes the measured distribution.

The hard question is:

> **Why does the native receiver/target comparison sweep generate the observed Planck statistical relation?**

Only then ask:

> **Why does a changed target/receiver resolution ratio shift the characteristic scale while the same native rule continues to generate the corresponding distribution?**

Do not claim either is already quantitatively derived.

## Toy-curve boundary

If mentioning:

`y = x^2 sqrt(1 - x^2)`

state clearly that it only shows that a bounded state-multiplicity/support trade can produce a peaked distribution.

It is **not** a Planck derivation.

The missing high-resolution/statistical occupancy rule remains open.

Do not import an exponential merely to fit blackbody data.

## Anisotropy

Use only as a qualitative candidate: broad coarse floor + tiny consequential retained distinctions.

State that measured angular power spectrum/acoustic peaks are not yet derived.

## Google Flow 16:9 hero prompt

Create a 16:9 editorial science illustration with no text.

Show a vast smooth cosmic background spanning nearly the whole frame, broad and low-detail, with tiny subtle ripples. In the foreground, dense branching structures recursively fold inward into finer nested detail, suggesting fine relational density compounding much faster than the background.

Both broad and fine structures should arise from one continuous equal-support ground so the image does not imply that the CMB receives less energy or budget.

Add a subtle measurement motif rather than a transported thermal curve: a receiver-like sequence of nested comparison scales or translucent frequency-range bands sampling the same broad background, with a statistical hump emerging only as the accumulated measurement result.

Do **not** depict one literal spectrum being carried unchanged from past to present.

Add tiny anisotropy speckles to the smooth background.

Avoid literal runners, batteries, explosion imagery, giant thermometers, equations, galaxy wallpaper, text labels, clocks, or a fading/dead CMB. Leave generous negative space for title.

## Ending

> **The CMB did not run out of budget. It simply lost the resolution race.**
